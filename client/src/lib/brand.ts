// ─── Brand / nome applicazione ────────────────────────────────────────────────
// Cambia APP_NAME qui per personalizzare il nome mostrato in tutta l'app
// (login, sidebar, esportazioni). Per il <title> del browser modifica anche
// `client/index.html`.
export const APP_NAME = "Gestionale Magazzino";

// Riga corta opzionale per l'header (es. nome locale, città).
// Lascia stringa vuota per nasconderla.
export const APP_TAGLINE = "";
